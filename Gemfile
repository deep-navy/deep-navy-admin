source "https://rubygems.org"

# GitHub Actions builds the site and uploads the rendered Pages artifact, so
# use maintained Jekyll directly instead of the legacy github-pages dependency
# set (Jekyll 3 and end-of-life Ruby Sass).
gem "jekyll", "~> 4.4.1"

# These standard libraries are gems on current Ruby releases.
gem "bigdecimal"
gem "csv"
