# deep-navy-admin

Static Jekyll admin application shell. The shared platform API enforces founder
or administrator authorization, MFA, short sessions, and audit logging for every
operation. This repository contains no secrets and is marked `noindex`.

